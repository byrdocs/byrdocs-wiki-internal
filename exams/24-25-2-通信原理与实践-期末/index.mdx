---
时间: 2024-2025学年第二学期
科目: 通信原理与实践
阶段: 期末
类型: 本科
学院:
- 未来学院
来源: f7e2acfc76e0b0c6be8ca48ae7a20846
答案完成度: 残缺
---

## 一、（20分）单项选择，每题1分
1. 以下调制方式中，频带利用率最高的是 <Slot item="1"/>，实现同样抗噪能力消耗功率最大的是 <Slot item="2"/>。

<Choices item="1">
    <Option>SSB</Option>
    <Option>DSB-SC</Option>
    <Option>AM</Option>
    <Option>FM</Option>
</Choices>

<Choices item="2">
    <Option>SSB</Option>
    <Option>DSB-SC</Option>
    <Option>AM</Option>
    <Option>FM</Option>
</Choices>

2. 均值为零的双极性 NRZ 信号通过调幅指数为 1 的 AM 调制器，输出是 <Slot item="3"/> 信号；通过 FM 调制器，输出是 <Slot item="4"/> 信号；通过 DSB-SC 调制器，输出是 <Slot item="5"/> 信号；先差分编码，再通过 DSB-SC 调制器，输出是 <Slot item="6"/> 信号。这四种调制方式中，只能相干解调的是 <Slot item="7"/>。

<Choices item="3">
    <Option>OOK</Option>
    <Option>2FSK</Option>
    <Option>BPSK</Option>
    <Option>DPSK</Option>
</Choices>

<Choices item="4">
    <Option>OOK</Option>
    <Option>2FSK</Option>
    <Option>BPSK</Option>
    <Option>DPSK</Option>
</Choices>

<Choices item="5">
    <Option>OOK</Option>
    <Option>2FSK</Option>
    <Option>BPSK</Option>
    <Option>DPSK</Option>
</Choices>

<Choices item="6">
    <Option>OOK</Option>
    <Option>2FSK</Option>
    <Option>BPSK</Option>
    <Option>DPSK</Option>
</Choices>

<Choices item="7">
    <Option>OOK</Option>
    <Option>2FSK</Option>
    <Option>BPSK</Option>
    <Option>DPSK</Option>
</Choices>

3. 若 BPSK 的误比特率是 0.001，则 2DPSK 相干解调差分译码后的误比特率是 <Slot item="8"/>。

<Choices item="8">
    <Option>0.0005</Option>
    <Option>0.001</Option>
    <Option>0.002</Option>
    <Option>0.003</Option>
</Choices>

4. 在滚降系数相同的条件下，QPSK 的 <Slot item="9"/> 比 OQPSK 大。

<Choices item="9">
    <Option>包络起伏</Option>
    <Option>码间干扰</Option>
    <Option>频带利用率</Option>
    <Option>误比特率</Option>
</Choices>

5. 信源信息量的大小完全体现在 <Slot item="10"/> 的大小上。

<Choices item="10">
    <Option>汉明重叠</Option>
    <Option>数据速率</Option>
    <Option>自相关函数</Option>
    <Option>熵</Option>
</Choices>

6. 设 AWGN 信道的带宽为 $B=1\,\mathrm{kHz}$，信号功率为 $S=10\,\mathrm W$、噪声单边功率谱密度为 $N_0=0,01\,\mathrm{W/Hz}$，则该信道的容量是 <Slot item="11"/> $\mathrm{kbit/s}$。固定 $S$ 与 $N_0$ 不变，无限增大信道带宽 $B$。当 $B$ 趋于无穷时，信道容量将趋于 <Slot item="12"/> $\mathrm{kbit/s}$。

<Choices item="11">
    <Option>$\ln2$</Option>
    <Option>$1$</Option>
    <Option>$\frac1{\ln2}$</Option>
    <Option>$\frac{10}{\ln2}$</Option>
</Choices>

<Choices item="12">
    <Option>$\ln2$</Option>
    <Option>$1$</Option>
    <Option>$\frac1{\ln2}$</Option>
    <Option>$\frac{10}{\ln2}$</Option>
</Choices>

7. 若 BSC 信道的差错概率为 $p$，则其信道容量为 <Slot item="13"/> $\mathrm{bit/symbol}$，其中 $h_2(p)$ 是二元熵函数。

<Choices item="13">
    <Option>$1+h_2(p)$</Option>
    <Option>$1-h_2(p)$</Option>
    <Option>$h_2(p)$</Option>
    <Option>$p\cdot h_2(p)$</Option>
</Choices>

8. 在无线通信中，信道编码器的输出一般先要经过一个交织器，以使信道差错 <Slot item="14"/>。

<Choices item="14">
    <Option>集中</Option>
    <Option>消失</Option>
    <Option>分散</Option>
    <Option>加倍</Option>
</Choices>

9. $(n,k)$ 分组码的生成矩阵有 <Slot item="15"/> 行，校验矩阵有 <Slot item="16"/> 行。若要求其具有纠正 $2\,\mathrm{bit}$ 错的能力，则最小码距至少应当是 <Slot item="17"/>。

<Choices item="15">
    <Option>$n$</Option>
    <Option>$k$</Option>
    <Option>$n-k$</Option>
    <Option>$r$</Option>
</Choices>

<Choices item="16">
    <Option>$n$</Option>
    <Option>$k$</Option>
    <Option>$n-k$</Option>
    <Option>$r$</Option>
</Choices>

<Choices item="17">
    <Option>$3$</Option>
    <Option>$5$</Option>
    <Option>$7$</Option>
    <Option>$9$</Option>
</Choices>

10. 若 $(7,4)$ 循环码的生成多项式为 $x^3+x+1$，则信息分组 $1110$ 对应的系统码码字为 <Slot item="18"/>。

<Choices item="18">
    <Option>$1110000$</Option>
    <Option>$1110001$</Option>
    <Option>$1110010$</Option>
    <Option>$1110100$</Option>
</Choices>

11. 某 DSSS 系统的扩频因子是 $16$。其发送信号的带宽是常规未扩频系统的 <Slot item="19"/> 倍。

<Choices item="19">
    <Option>$0$</Option>
    <Option>$8$</Option>
    <Option>$12$</Option>
    <Option>$16$</Option>
</Choices>

12. 5G 系统采用 OFDM 的目的是为了 <Slot item="20"/>。

<Choices item="20">
    <Option>降低包络起伏</Option>
    <Option>实现非相干解调</Option>
    <Option>克服多径传播引起的 ISI 问题</Option>
    <Option>克服终端运动带来的多普勒频移问题</Option>
</Choices>

## 二、（12分）
设有模拟基带信号 $m(t)=2\cos20\pi t-\sin10\pi t$，用其对高频载波模拟器幅度调制得到已调信号 $s(t)=m(t)\cos2\pi f_ct-\hat m(t)\sin2\pi f_ct$。$s(t)$ 通过双边功率谱密度为 $\frac{N_0}2$ 的 AWGN 信道传输。
1. 计算 $m(t)$ 的功率。
2. 写出 $m(t)$ 的希尔伯特变换 $\hat m(t)$ 的表达式。
3. 画出对 $s(t)$ 进行相干解调的解调框图。
4. 假设载波恢复理想，求解调输出信噪比。
## 三、（12分）
某二进制数字调制系统在 $[0,T_b]$ 时间内等概发送 $s_1(t)=\sin\left(\frac{2\pi t}{T_b}\right)$ 或 $s_2(t)=\sin\left(\frac{4\pi t}{T_b}+\pi\right)$。发送信号通过 AWGN 信道传输，噪声的双边功率谱密度为 $\frac{N_0}2$。
1. 计算 $s_1(t)$ 和 $s_2(t)$ 的能量。
2. 画出最佳接收框图。
3. 写出发送 $s_2(t)$ 条件下判决量的均值、方差。
4. 写出最佳判决门限，求平均误比特率。
## 四、（12分）
某 8 进制调制在归一化正交基下的星座点可以表示为 $s=A\mathrm e^{j\theta}$，其中 $A$ 取值于 $\{1,2\}$，$\theta$ 取值于 $\left\{0,\frac\pi2,\pi,\frac{3\pi}2\right\}$。假设各星座点等概出现且通过 AWGN 信道传输。
1. 画出星座图。
2. 求出平均符号能量以及星座点之间的最小欧氏距离。
3. 画出每个星座点的最大似然判决域。
## 五、（12分）
<Figure src="题图5.svg" float>
    第五题图
</Figure>

某四电平均匀量化器的输入 $X$ 的概率密度函数 $p(x)$ 如图所示，图中的黑色圆点表示量化输出 $Y$ 的四个量化电平 $y_1,y_2,y_3,y_4$。令 $U=\operatorname{sgn}(x)$, $V=\left|Y\right|$。试求
1. 熵 $H(Y),H(U),H(V)$；
2. 互信息 $I(Y;U),I(Y;V)$；
3. $E[X^2],E[Y^2],E[XY],E[(X-Y)^2]$。
## 六、（12分）
以 $g_1(x)=x^2+x+1$, $g_2(x)=x^2$, $g_3(x)=x^2+1$ 为生成多项式，构成一个 $(3,1,3)$ 卷积码。画出该卷积码的编码器框图、状态转移图，并写出输入为 $1011100$ 时对应的编码输出。
## 七、（20分）
设计一个无线通信系统，要求能传频谱范围在 $300-3400\,\mathrm{Hz}$ 范围内的话音信号，传输信道为带通 AWGN 信道。要求：
1. 给出具体设计（包括框图及主要参数）；
2. 对所设计方案有一定的性能评价（抗噪声能力、频谱效率、技术先进性等），并说明提出此设计的理由。
